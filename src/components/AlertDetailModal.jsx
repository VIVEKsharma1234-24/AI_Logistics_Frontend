import { Clock3, MapPin, X } from 'lucide-react'
import RiskBadge from './RiskBadge'

const severityStyles = { CRITICAL: 'border-[#e4513f] bg-[#fffaf8]', HIGH: 'border-[#e98932] bg-[#fffdf9]', MODERATE: 'border-[#e2bd27] bg-[#fffef8]', LOW: 'border-[#28a879] bg-[#fbfefd]' }

export default function AlertDetailModal({ alert, onClose }) {
  if (!alert) return null
  return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-[#102f3b]/60 p-4" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose() }}><div role="dialog" aria-modal="true" aria-labelledby="alert-detail-title" className={`max-h-[90vh] w-full max-w-xl overflow-y-auto border-t-4 bg-white p-6 shadow-2xl sm:p-8 ${severityStyles[alert.severity]}`}><div className="flex items-start justify-between gap-4"><div><p className="mb-2 text-[10px] font-bold uppercase tracking-[0.18em] text-[#ef6847]">Alert detail</p><h2 id="alert-detail-title" className="display-font text-2xl font-bold text-[#173747]">{alert.title}</h2></div><button type="button" onClick={onClose} aria-label="Close alert details" className="flex h-9 w-9 items-center justify-center border border-[#dce5e7] text-[#66808a] hover:bg-[#f4f7f8]"><X size={18} /></button></div><div className="mt-5 flex flex-wrap items-center gap-3"><RiskBadge level={alert.severity.charAt(0) + alert.severity.slice(1).toLowerCase()} /><span className="text-xs font-bold text-[#66808a]">{alert.status}</span></div><p className="mt-5 text-sm leading-relaxed text-[#4d6872]">{alert.message}</p><div className="mt-6 grid gap-4 border-y border-[#edf1f2] py-5 sm:grid-cols-2"><div className="flex items-start gap-3"><MapPin size={17} className="mt-0.5 text-[#ef6847]" /><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-[#8aa0a6]">Location</p><p className="mt-1 text-sm font-semibold text-[#284c58]">{alert.location}</p></div></div><div className="flex items-start gap-3"><Clock3 size={17} className="mt-0.5 text-[#ef6847]" /><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-[#8aa0a6]">Issued</p><p className="mt-1 text-sm font-semibold text-[#284c58]">{alert.issuedAt}</p></div></div></div><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-[#8aa0a6]">Response guidance</p><p className="mt-2 text-sm leading-relaxed text-[#66808a]">{alert.response}</p></div><p className="mt-6 text-[11px] text-[#9aadb2]">Source: {alert.source} · Mock alert for interface development</p></div></div>
}